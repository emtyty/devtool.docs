---
title: number_base_convert
description: Convert numbers between decimal, hexadecimal, binary, and octal (BigInt-safe).
---

import { Aside } from '@astrojs/starlight/components';

## Purpose

Convert numbers between decimal, hexadecimal, binary, and octal (BigInt-safe).

## Parameters

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| (see source) | — | — | — |

## Return Shape

```json
{
  "success": true,
  "data": {},
  "summary": "..."
}
```

## Example

### Input

```json
{}
```

### Output

```json
{ "success": true, "data": {}, "summary": "" }
```

## Notes

- See `mcp/src/tools/number-base-convert.ts` for the full parameter schema.
