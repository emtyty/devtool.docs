---
title: regex_test
description: Test a regular expression against input — returns matches, captures, named groups, and a replace result.
---

import { Aside } from '@astrojs/starlight/components';

## Purpose

Test a regular expression against input — returns matches, captures, named groups, and a replace result.

## Parameters

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| (see source) | — | — | — |

## Return Shape

```json
{
  "success": true,
  "data": {},
  "summary": "..."
}
```

## Example

### Input

```json
{}
```

### Output

```json
{ "success": true, "data": {}, "summary": "" }
```

## Notes

- See `mcp/src/tools/regex-test.ts` for the full parameter schema.
