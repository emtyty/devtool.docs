---
title: url_parse
description: Parse a URL into its components (scheme, host, path, query params) and manipulate query strings.
---

import { Aside } from '@astrojs/starlight/components';

## Purpose

Parse a URL into its components (scheme, host, path, query params) and manipulate query strings.

## Parameters

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| (see source) | — | — | — |

## Return Shape

```json
{
  "success": true,
  "data": {},
  "summary": "..."
}
```

## Example

### Input

```json
{}
```

### Output

```json
{ "success": true, "data": {}, "summary": "" }
```

## Notes

- See `mcp/src/tools/url-parse.ts` for the full parameter schema.
